ext {

    appVersionCode = 100 + Integer.parseInt(
            'git rev-list --count HEAD'.execute([], project.rootDir).text.trim())
    appVersionName = 'git describe --tags'.execute([], project.rootDir).text.trim()
}